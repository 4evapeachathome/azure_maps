import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RiskAssessmentSSripaPage } from './risk-assessment-ssripa.page';

describe('RiskAssessmentSSripaPage', () => {
  let component: RiskAssessmentSSripaPage;
  let fixture: ComponentFixture<RiskAssessmentSSripaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RiskAssessmentSSripaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
